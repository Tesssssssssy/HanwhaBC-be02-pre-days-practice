package day02;

public class Ex01 {
    public static void main(String[] args) {
        Integer[] arr = new Integer[10];
        // 클래스의 객체로 만들어진다.

        arr[0] = 10;
        arr[1] = 20;
        arr[2] = 30;
        arr[3] = 40;
        arr[4] = 50;
        // 너무 번거로움.

        int[] unit = {50000, 10000, 1000, 500, 10, 50, 10, 1};
        // 초기에 배열을 만들 때 값을 저장해서 만들 수 있다.
        // 배열은 반복문과 함께 사용되는 경우가 많다.
        // 배열에 접근할 때 index 번호로 접근하기 때문에.








    }
}
